function scrollToTop() {
    window.scrollTo({
       top: 0, /*The top parameter specifies the vertical position to scroll to, which is set to 0 to scroll to the top of the page.*/
      behavior: "smooth" /*The behavior parameter determines the scrolling behavior. The value "smooth" makes the scrolling smooth and animated.*/
  });
}